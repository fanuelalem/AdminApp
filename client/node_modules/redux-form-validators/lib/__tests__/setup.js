"use strict";

var _index = _interopRequireDefault(require("../index"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

global.ValidatorsFormatMessage = _index["default"].formatMessage;

_index["default"].formatMessage = function (msg) {
  return msg;
};

global.FileList = function (params) {
  params = params || {
    length: 0
  };
  Object.assign(this, params);
  this.length = params.length;
};

global.File = function (params) {
  Object.assign(this, params);
};

console.__lastErrors = [];

console.error = function () {
  var _arguments = arguments;

  console.__lastErrors.push(arguments);

  console.__lastErrorIncludes = function (txt) {
    return [].slice.call(_arguments).join(' ').indexOf(txt) >= 0;
  };
};